# AI 项目开发边界与执行协议

## 全局要求

- 所有回答使用中文。
- 计划阶段不执行构建，避免影响效率。

## 前端技术栈识别

- `apps/web` 是 React 19 + React Router 8 + Vite 项目，不是 Vue 项目。
- 处理 `apps/web` 时，不得加载或应用 Vue、Pinia、VueUse、uni-app 相关技能。
- Vite 是构建工具，不能作为项目使用 Vue 的判断依据。
- 判断前端框架时，必须以 `apps/web/package.json`、源码文件扩展名和真实导入为准。

## 本地开发与验证约定

- 本地前端开发服务默认由用户维护，地址为 `http://localhost:10001`。
- 需要页面验证时，优先复用已经运行的 `10001` 端口服务；可以检查服务是否可访问，但不得重复启动项目。
- 如果 `10001` 端口不可访问且任务必须依赖运行环境，应先说明情况并取得用户确认，再启动服务。
- 未经用户明确要求，不得停止、重启或替换用户正在运行的开发进程，也不得擅自更改端口。
- 完成同一任务的全部代码修改后，统一执行一次 lint；不得在每个小步骤或每次局部修改后重复执行。
- lint 发现问题并完成修复后，可以针对修复内容再次验证；仅修改文档或项目规则时不执行 lint。
- 构建仅在用户明确要求、发布前验证或排查构建问题确有必要时执行；计划阶段不得执行构建。

## Git 协作约定

- 未经用户明确要求，不得执行 `git add`、`git commit` 或 `git push`。
- 完成修改后保留工作区差异，由用户自行检查 `git diff`；AI 不得主动执行、展示或解读 `git diff`。
- 默认不执行 Git 写操作；其他 Git 操作必须由用户明确要求，不得以“自动体检”为由执行与当前任务无关的操作。
- 提交信息使用中文描述，但前缀遵循规范化提交格式，例如 `docs: 拆分项目记忆文件`。

## Go 开发约定

- 用户不熟悉 Go。涉及 JWT、登录认证、中间件、上下文、权限校验等机制时，实施前必须先用中文说明目标、请求流程、目录与职责、准备新增的依赖或方法、安全风险和验证方式。
- 上述 Go 方案必须先完成规划，再按用户确认的范围实施；规划阶段不得构建。
- 所有具名 Go 函数和方法都必须添加中文注释，说明它负责什么；复杂流程还应在关键步骤补充必要注释，但不得逐行复述代码。
- Go 注释应优先使用对应函数或方法名开头，符合 Go 文档注释习惯。

## 一、核心原则

任务是按照用户明确提出的需求完成开发，不是重新设计项目。

始终遵循：

1. 使用满足当前需求的最小实现。
2. 优先复用现有代码、页面、组件、接口、数据库表和部署配置。
3. 不为了“架构完整”“以后可能用到”“看起来更专业”而增加内容。
4. 不主动扩大需求范围。
5. 不修改与当前任务无关的代码。
6. 用户没有提出的功能，默认不实现。
7. 无法确认是否需要的内容，默认不新增。
8. 不以“最佳实践”为理由覆盖项目现有的合理实现。

判断优先级：

用户明确需求 > 当前代码和数据库事实 > 项目记忆 > 项目现有规范 > 通用最佳实践。

## 二、禁止过度设计

除非当前需求确实无法完成，或者用户明确要求，否则禁止新增：

- 新的架构层
- 新的设计模式
- 新的公共抽象
- 新的基础类
- 新的工具类
- 新的中间件
- 新的依赖包
- 新的服务
- 新的 Docker 容器
- 新的部署组件
- 新的配置中心
- 新的缓存层
- 新的消息队列
- 新的权限体系
- 新的状态管理方案
- 新的数据库表
- 新的数据库字段
- 新的页面
- 新的菜单
- 新的按钮
- 新的接口
- 新的控制器
- 新的 Service 方法
- 新的 Repository 方法

一个简单方法可以解决的问题，不要拆成多层。

一个现有模块可以承载的问题，不要新建模块。

一个现有字段可以表达的问题，不要新增字段。

当前没有复用需求的代码，不要提前抽象成公共组件。

不要为未知的未来需求预留复杂设计。

## 三、任务执行前的强制流程

每次准备修改代码前，必须先执行以下步骤。

### 第一步：分析问题

明确：

- 用户真正要求解决的问题
- 当前问题发生的位置
- 本次任务包含什么
- 本次任务不包含什么
- 是否涉及前端、后端、数据库或部署
- 是否存在数据安全、兼容性或破坏性风险

不得在没有理解问题前直接修改代码。

### 第二步：读取项目记忆

先读取：

1. 项目根目录的 `AGENTS.md`
2. `.ai/memory/index.md`
3. 根据 `.ai/memory/index.md` 路由读取与当前任务有关的记忆文件
4. 当前模块附近的说明文件
5. 对应的历史数据库迁移文件

只读取与当前任务相关的记忆，不需要加载全部无关信息。

在制定方案时，需要明确说明本次使用了哪些项目记忆。

### 第三步：检查真实实现

不能只依赖记忆。

必须检查：

- 当前真实代码
- 当前目录结构
- 当前接口实现
- 当前数据库结构
- 当前数据库迁移记录
- 当前部署配置
- 相关调用关系

当记忆和真实代码冲突时，以当前代码、当前数据库结构和已经执行的迁移为事实依据，同时指出记忆已经过期。

### 第四步：制定最小方案

实施前先给出简洁计划，包括：

- 问题原因
- 最小解决方案
- 准备修改的文件
- 准备影响的接口
- 准备影响的数据库表和字段
- 是否需要新增文件、方法、字段或依赖
- 风险和验证方式

如果不需要新增内容，明确写明“不新增”。

如果用户已经明确要求直接执行，可以在完成分析和计划后继续实施，不需要重复询问。

### 第五步：按照计划实施

实施过程中不得擅自扩大方案。

如果发现原计划无法完成，应先重新分析并更新计划，不能直接增加新的页面、接口、表、字段或架构层。

## 四、前端开发边界

前端只实现用户明确提出的页面和交互。

### 前端命名与注释约定

- 局部变量和状态优先使用包含业务对象与用途的名称，避免在复杂组件中使用脱离上下文后含义不清的 `items`、`loading`、`editing` 等通用名称。
- 同一业务流程的表单、列表、抽屉和分页状态应按职责分组；分组注释需要说明该组职责，并列出主要变量分别保存什么内容、在什么场景更新。
- 具名业务方法使用中文 JSDoc 说明职责、主要输入、关键处理过程、副作用和返回结果；类型字段和公共 Props 继续使用中文 JSDoc。
- 简单 setter、明显的临时变量和自解释表达式不添加与代码重复的逐行注释；复杂分支只在业务原因不直观时补充关键说明。

禁止擅自增加：

- 页面
- 菜单
- Tab
- 表单字段
- 搜索条件
- 操作按钮
- 弹窗
- 状态
- 权限判断
- 动画
- 空状态
- 引导流程
- 批量操作
- 导入导出
- 用户没有要求的响应式适配

优先使用项目现有：

- 组件
- Hooks
- 请求封装
- 状态管理
- 样式方案
- 表格和表单规范

不要因为现有代码“不够优雅”而重写整个页面。

## 五、后端开发边界

后端只提供当前功能需要的最小接口能力。

禁止擅自增加：

- Controller
- API 路由
- Service
- Repository
- DTO
- VO
- Entity
- 中间件
- 定时任务
- 缓存
- 事件机制
- 消息队列
- 审计模块
- 通用 CRUD
- 用户没有要求的扩展接口

修改现有方法能够完成时，不新增相似方法。

现有接口能够合理扩展时，不创建重复接口。

不要为了分层而分层，不要制造只有一层转发逻辑的方法。

## 六、部署文件边界

除非用户明确要求，否则禁止修改：

- Dockerfile
- docker-compose.yml
- Nginx 配置
- CI/CD
- 环境变量结构
- 服务器目录
- 端口
- 域名
- 网络配置
- 数据库连接方式
- 日志收集方式

禁止擅自增加容器、服务、反向代理、监控系统或部署脚本。

修复应用代码问题时，不得通过修改部署架构绕过问题。

## 七、数据库最高优先级规则

数据库属于高风险区域，必须保守处理。

### 1. 数据库事实来源

数据库结构的事实来源依次为：

1. 当前实际数据库结构
2. 已执行的 Migration
3. 项目实体或模型代码
4. `.ai/memory/database.md`
5. 其他说明文档

不得仅凭模型代码或记忆猜测数据库结构。

### 2. 修改数据库前必须检查

修改前必须确认：

- 当前表结构
- 字段类型
- 是否允许 NULL
- 默认值
- 索引
- 唯一约束
- 外键
- 数据量
- 当前字段使用位置
- 历史 Migration
- 是否会影响旧数据
- 是否会影响现有接口

### 3. 禁止随意修改

没有用户明确要求时，禁止：

- 新建表
- 新增字段
- 删除字段
- 重命名字段
- 修改字段类型
- 修改字段长度
- 修改默认值
- 修改 NULL 约束
- 新增或删除索引
- 新增或删除唯一约束
- 新增或删除外键
- 拆表
- 合表
- 数据回填
- 批量更新数据
- 删除数据

不得因为代码实现不方便就修改数据库。

不得在多个方案之间反复修改数据库结构。

### 4. Migration 规则

所有数据库结构变化必须通过新的 Migration 完成。

禁止修改已经执行或已经进入共享环境的历史 Migration。

Migration 固定约定：

- 数据库结构变化历史的唯一来源是 `apps/service/migrations`。
- Migration 工具固定为 Goose v3，且只使用 SQL Migration。
- DBHub 仅用于只读检查，禁止通过 DBHub 执行写入或 DDL。
- 修改数据库前必须比较当前真实结构、已有 Migration 和相关 Go Model。
- 禁止使用 GORM AutoMigrate，禁止在 Go 服务启动时自动执行 Migration。
- AI 生成 Migration 后必须停止，取得用户明确批准后才能执行。
- 不得创建用户没有明确要求的表、字段、索引或外键。
- DROP、删除字段、字段缩短、修改字段类型和数据回填必须单独说明风险，并再次取得用户明确批准。

每个数据库变更必须包含：

- 修改原因
- Up SQL
- 回滚方案
- 影响范围
- 兼容性说明
- 数据处理说明
- 验证 SQL

数据库变更应保持最小化，一个需求只修改必要内容。

### 5. 数据库执行权限

默认只允许 AI：

- 查看数据库结构
- 执行 SELECT
- 执行 SHOW
- 执行 DESCRIBE
- 执行 EXPLAIN
- 生成 Migration 草案
- 生成验证 SQL
- 生成回滚 SQL

默认禁止 AI 直接执行：

- INSERT
- UPDATE
- DELETE
- TRUNCATE
- ALTER
- CREATE
- DROP
- RENAME
- GRANT
- REVOKE

生产数据库禁止 AI 直接写入。

涉及结构修改或数据修改时，AI 只能先生成变更方案和 Migration，等待明确批准后再执行。

## 八、项目记忆规则

项目记忆是项目事实记录，不是 AI 自由发挥的笔记。

项目记忆存放在：

`.ai/memory/`

建议文件：

- `index.md`：记忆索引
- `architecture.md`：当前真实架构、技术栈和前后端边界
- `frontend.md`：前端已实现范围、页面组件和验证约定
- `backend.md`：Go 服务、接口、中间件、启动与服务边界
- `project.md`：项目目标和边界
- `database.md`：表用途、字段语义和数据库约束
- `auth-rbac.md`：多租户、认证和 RBAC 权限边界
- `audit-log.md`：系统日志与操作审计事实
- `media-branding.md`：平台品牌、图片库和媒体能力
- `decisions.md`：已经确定的重要技术决定
- `modules.md`：各模块职责

### 记忆读取规则

每次任务开始时：

1. 先读取 `index.md`
2. 根据 `index.md` 的读取策略选择对应记忆文件
3. 再检查真实代码和数据库
4. 根据事实制定方案

不得只依赖对话历史或模型自身记忆。

项目不长期维护开发计划文件。每次业务任务根据用户当前需求单独制定方案，完成后不把
执行流水账写入项目记忆；需要追溯时查看 Git 历史。

### 记忆写入规则

只记录长期有效的信息：

- 已经确定的业务规则
- 已经确定的技术选择
- 模块边界
- 数据库字段真实含义
- 明确禁止修改的内容
- 用户确认的设计决定

不得记录：

- 未确认猜测
- 临时分析
- 已经过期方案
- AI 自己提出但用户没有确认的设计
- 可以从代码直接看出的琐碎信息

每次任务结束后，只有在项目事实发生变化时才更新记忆。

更新记忆时不得删除仍然有效的历史决定。

## 九、变更控制

每次完成任务后，必须输出：

1. 问题原因
2. 使用了哪些项目记忆
3. 实际修改了哪些文件
4. 新增了哪些内容
5. 删除了哪些内容
6. 是否修改数据库
7. 是否修改部署配置
8. 如何验证
9. 尚未解决的问题

如果没有修改数据库，明确写：

“本次未修改数据库结构和数据。”

如果没有新增页面、接口、方法或字段，明确写：

“本次未新增页面、接口、控制器方法或数据库字段。”

不得声称已经完成未实际执行的测试。

## 十、破坏性操作保护

遇到以下情况时，不得自行执行：

- 删除数据库数据
- 删除表或字段
- 修改字段类型
- 修改唯一约束
- 修改权限模型
- 修改登录认证
- 修改生产部署
- 批量更新历史数据
- 不可逆 Migration
- 大范围重构

此时只能提供：

- 当前问题分析
- 可选方案
- 影响范围
- 推荐方案
- 回滚方案

必须得到用户明确批准后才能执行。

## 十一、最终判断标准

每次准备新增任何东西前，先问自己：

1. 用户明确要求了吗？
2. 当前需求没有它就无法完成吗？
3. 现有代码真的不能复用吗？
4. 是否会增加维护成本？
5. 是否属于为了设计而设计？
6. 是否会影响数据库稳定性？
7. 是否可以使用更小的改动完成？

只要无法明确证明“必须新增”，就不要新增。
